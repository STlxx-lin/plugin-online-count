name: Build and Release

on:
  push:
    tags:
      - 'v*'
    branches:
      - main
  workflow_dispatch:

jobs:
  build-and-release:
    name: Build & Release NocoBase Plugin
    runs-on: ubuntu-latest
    permissions:
      contents: write
      packages: write

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4
        with:
          path: packages/plugins/@nocobase/plugin-online-count

      - name: Setup Node.js Environment
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Generate Root package.json
        run: |
          cat << 'EOF' > package.json
          {
            "name": "nocobase-build-env",
            "private": true,
            "workspaces": [
              "packages/*/*",
              "packages/*/*/*"
            ],
            "scripts": {
              "nocobase": "nocobase-v1"
            },
            "dependencies": {
              "@nocobase/app": "2.2.0-beta.15",
              "@nocobase/cli": "2.2.0-beta.15",
              "@nocobase/build": "2.2.0-beta.15",
              "@nocobase/devtools": "2.2.0-beta.15"
            },
            "resolutions": {
              "@umijs/bundler-webpack": "4.7.13",
              "@umijs/preset-umi": "4.7.13",
              "@umijs/renderer-react": "4.7.13",
              "@umijs/utils": "4.7.13",
              "cytoscape": "3.28.0",
              "@types/react": "18.3.18",
              "@types/react-dom": "^18.0.0",
              "react-router-dom": "^6.30.1",
              "react-router": "^6.30.1",
              "antd": "5.24.2",
              "async": "^3.2.6",
              "rollup": "4.24.0",
              "semver": "^7.7.1"
            }
          }
          EOF

      - name: Generate Configuration Files
        run: |
          cat << 'EOF' > lerna.json
          {
            "version": "0.1.2",
            "npmClient": "yarn",
            "useWorkspaces": true
          }
          EOF
          cat << 'EOF' > tsconfig.json
          {
            "extends": "./tsconfig.paths.json",
            "compilerOptions": {
              "esModuleInterop": true,
              "moduleResolution": "node",
              "jsx": "react",
              "target": "esnext",
              "module": "esnext",
              "allowJs": true,
              "noUnusedLocals": false,
              "preserveConstEnums": true,
              "skipLibCheck": true,
              "sourceMap": true,
              "inlineSources": true,
              "resolveJsonModule": true,
              "declaration": true,
              "experimentalDecorators": true,
              "downlevelIteration": true,
              "baseUrl": "."
            },
            "exclude": [
              "node_modules",
              "**/dist",
              "**/lib",
              "**/*.js",
              "**/*.d.ts"
            ]
          }
          EOF
          cat << 'EOF' > tsconfig.paths.json
          {
            "compilerOptions": {
              "paths": {
                "@nocobase/plugin-online-count/client": [
                  "packages/plugins/@nocobase/plugin-online-count/src/client"
                ],
                "@nocobase/plugin-online-count/client-v2": [
                  "packages/plugins/@nocobase/plugin-online-count/src/client-v2"
                ],
                "@nocobase/plugin-online-count/package.json": [
                  "packages/plugins/@nocobase/plugin-online-count/package.json"
                ],
                "@nocobase/plugin-online-count": [
                  "packages/plugins/@nocobase/plugin-online-count/src"
                ]
              }
            }
          }
          EOF

      - name: Create Env Files
        run: |
          touch .env
          touch .env.example
          touch .env.e2e.example

      - name: Install Dependencies with Retry
        run: |
          yarn config set network-timeout 600000
          yarn config set registry https://registry.npmjs.org/
          
          n=0
          until [ "$n" -ge 4 ]
          do
            echo "Running yarn install (attempt $((n+1)))..."
            yarn install --network-timeout 600000 && break
            n=$((n+1))
            if [ "$n" -lt 4 ]; then
              echo "yarn install failed due to network/registry glitch, retrying in 5 seconds..."
              sleep 5
            else
              echo "ERROR: yarn install failed after 4 attempts."
              exit 1
            fi
          done

      - name: Get Plugin Version
        id: plugin_version
        run: |
          VERSION=$(node -p "require('./packages/plugins/@nocobase/plugin-online-count/package.json').version")
          echo "version=${VERSION}" >> $GITHUB_OUTPUT
          echo "RELEASE_VERSION=${VERSION}" >> $GITHUB_ENV
          mkdir -p /tmp/release-artifacts

      - name: Build & Package Plugin
        run: |
          yarn nocobase build @nocobase/plugin-online-count
          yarn nocobase tar @nocobase/plugin-online-count
          
          # 复制由 NocoBase 官方内置打包器生成的标准规范 tgz 安装包
          cp storage/tar/@nocobase/plugin-online-count-*.tgz "/tmp/release-artifacts/plugin-online-count-v${RELEASE_VERSION}.tgz"
          
          cd packages/plugins/@nocobase/plugin-online-count
          zip -r "/tmp/release-artifacts/plugin-online-count-v${RELEASE_VERSION}.zip" \
            package.json README.md server.js server.d.ts client.js client.d.ts client-v2.js client-v2.d.ts dist

      - name: Upload Build Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: plugin-online-count-v${{ steps.plugin_version.outputs.version }}
          path: |
            /tmp/release-artifacts/plugin-online-count-v*.tgz
            /tmp/release-artifacts/plugin-online-count-v*.zip

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        if: startsWith(github.ref, 'refs/tags/')
        with:
          tag_name: ${{ github.ref_name }}
          name: Release ${{ github.ref_name }}
          draft: false
          prerelease: false
          generate_release_notes: true
          files: |
            /tmp/release-artifacts/plugin-online-count-v*.tgz
            /tmp/release-artifacts/plugin-online-count-v*.zip
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
